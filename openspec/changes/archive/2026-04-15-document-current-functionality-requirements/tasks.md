## 1. Baseline Current Behavior

- [x] 1.1 Review the frontend, service implementations, and published contracts to confirm the current profile lifecycle behavior captured in `profile-lifecycle`.
- [x] 1.2 Review the matching, websocket, Kafka, and date-generation flow to confirm the current behavior captured in `matching-workflow` and `location-and-date-generation`.

## 2. Finalize Capability Specs

- [x] 2.1 Refine `profile-lifecycle` scenarios so they stay aligned with the current runtime behavior and user-visible flow.
- [x] 2.2 Refine `matching-workflow` scenarios so they stay aligned with the current scheduler-driven and websocket-driven behavior.
- [x] 2.3 Refine `location-and-date-generation` scenarios so they stay aligned with current validation, venue generation, and fallback behavior.

## 3. Validate Change Readiness

- [x] 3.1 Run OpenSpec validation for `document-current-functionality-requirements` and fix any schema or formatting issues.
- [x] 3.2 Review the completed change artifacts with stakeholders and use them as the baseline for subsequent feature or cleanup proposals.
